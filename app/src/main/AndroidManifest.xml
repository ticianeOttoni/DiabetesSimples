<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ticiane.diabetessimples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistraMedicaoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaActivity" />
        <activity
            android:name=".ListaTipoMedidaActivity"
            android:label="@string/title_activity_lista_tipo_medida"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistraTipoMedidaActivity"
            android:label="@string/tipoMedida" />
    </application>

</manifest>